# Database Configuration
DB_NAME=pprdv
DB_USER=root
DB_PASS=
DB_HOST=localhost
DB_PORT=3306

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-here-make-it-long-and-random

# Session Configuration
SESSION_SECRET=your-super-secret-session-key-here-make-it-long-and-random

# Email Configuration (SMTP)
# To send emails via your personal email, set the following values:
# - MAIL_FROM: The email address that will appear as the sender (usually your personal email)
# - SMTP_HOST: The SMTP server for your email provider (e.g., smtp.gmail.com for Gmail)
# - SMTP_PORT: The SMTP port (587 for TLS, 465 for SSL, 25 for non-secure)
# - SMTP_SECURE: "true" for port 465 (SSL), "false" for port 587 (TLS)
# - SMTP_USER: Your personal email address (the one you want to send from)
# - SMTP_PASS: The app password or your email password (for Gmail, use an App Password if 2FA is enabled)

MAIL_FROM=your-personal-email@gmail.com
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-personal-email@gmail.com
SMTP_PASS=your-app-password

# Application URL
APP_URL=http://localhost:3000
# Environment
NODE_ENV=development

